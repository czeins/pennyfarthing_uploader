<div class="row text-center">
  <h1>Oh, so you'd like to create a new story, eh?</h1>
  <p class="tagline">Awesome possum. Let's get started.</p>
</div>

<div class="row text-center signin">
  <div class="col-lg-10 col-lg-offset-1">
    <%= form_for @story, url: "/stories" do |f| %>
      <div class="form-group">
        <%= f.text_field :title, placeholder: "title", class: 'form-control'%><br>

        <%= f.text_area raw(:html), placeholder: "your html", class: 'form-control',  rows: "12" %>
        <%= f.file_field :headerimg, class: 'form-control' %>
      </div>
        <%= f.submit "Add story", class: 'btn btn-success'%>
      </div>
    <% end %>
  </div>

</div>